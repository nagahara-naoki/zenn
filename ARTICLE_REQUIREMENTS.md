# 技術記事の設計・執筆ガイド

このファイルは、Zenn向けの技術記事を設計し、執筆し、公開前に見直すための共通ルールである。記事のテーマが変わっても、読者が「仕組みを理解し、自分のコードで判断・実践できる」状態へ到達することを目指す。

## このガイドの使い方

対象は、Zenn記事の執筆者とレビュー担当者である。AIを使う場合も、判断と公開の責任は記事の名義人が持つ。このガイドで「筆者」と書くときは記事の名義人を指し、AIや検証ツールを筆者に見立てない。

初回は「1. 完成した記事が満たす状態」「2. 執筆前に決めること」「11. 作成手順」を読む。執筆中は3〜10を必要に応じて参照し、公開前は12で確認する。13はAIへ依頼するときだけ使う。最初から全項目を記事へ盛り込む必要はない。

要件を満たすこと自体が目的ではない。読者の疑問を解消するために必要な説明、コード、図、表を選び、不要なものは入れない。数や型を機械的にそろえるより、理解に必要な情報が無理なくつながっていることを優先する。

もう一つ、このガイドでは「筆者の声」を大切にする。ここでいう声は、口語表現や感情語の多さではない。実際に観測した事実、迷った選択、採用しなかった案、言い切れない範囲を隠さず書くことだ。整いすぎた一般論よりも、そのテーマについて何を確かめ、どう判断したのかが見える記事を目指す。

AIらしさを減らすために、口調を崩したり、体言止めや一人称を足したりはしない。定型句と均一な構成を避け、事実の密度と判断の根拠を上げる。AI判定ツールの点数を完成条件にはしない。

優先順位は次のとおり。

1. 技術的に正しいこと
2. 読者の疑問に直接答えていること
3. 具体例から仕組みと判断基準を理解できること
4. 筆者の観測と判断が、無理のない言葉で伝わること
5. 文章や構成が整っていること

文章のきれいさを優先して、事実や筆者の実感を薄めない。

要件がぶつかった場合は、上位の項目を優先する。技術的正確性、体験を創作しないこと、未確認の内容を断定しないこと、中心となる主張の根拠を示すことは省略できない。見出し階層、コードの行数、図表の数などの形式は、読者の理解を助けないなら外して構わない。

本文中の要件は、次の強さで解釈する。

| 表現 | 扱い方 |
| --- | --- |
| 「〜する」「〜しない」 | 原則として満たす。条件付き要件を外す場合は、理由を設計メモへ残すこと |
| 「原則として」「基本とする」 | 迷ったときに採用する標準。テーマに合わなければ変更してよい |
| 「検討する」「候補になる」 | 記事への効果を判断し、不要なら採用しなくてよい |

## 1. 完成した記事が満たす状態

良質な技術記事は、中心課題に関係する次の質問に答えられる。記事の種類に合わない質問は、設計メモで `N/A`（該当なし）とし、理由を残す。

1. この記事は、どの問題を解決するのか。
2. 紹介する仕組みは、どのように動くのか。
3. コードを載せる場合、各段階で何が入力され、何が起き、何が返るのか。
4. どの条件で使うべきか。使わない方がよい条件は何か。
5. 失敗した場合、どこから確認すればよいか。
6. 仕様、ライブラリ固有の挙動、筆者の設計判断を区別できるか。

コードを載せる場合、コピーできるだけでは完成としない。読者がコードを変更したときに、結果を予測できるところまで説明する。

## 2. 執筆前に決めること

本文を書く前に、次の設計メモを作る。本文にすべて載せる必要はないが、執筆者は答えを持っていなければならない。

| 項目 | 決める内容 |
| --- | --- |
| 想定読者 | 初学者、実務経験者、特定技術の利用者など |
| 前提知識 | 読者が理解済みとみなす言語・ツール・概念 |
| 読者の困りごと | 検索するきっかけとなった疑問や不具合 |
| 読了後の状態 | 読者が説明・実装・判断できるようになること |
| 主メッセージ | 記事で最も伝えたい結論を一文で表したもの |
| 対象範囲 | 記事で扱うこと、扱わないこと |
| 動作条件 | 言語、ランタイム、ライブラリ、OSなどの前提 |
| 根拠 | 仕様、公式ドキュメント、実行結果などの確認先 |
| 筆者の材料 | 実際に試したこと、詰まった箇所、比較した案、手元のコードやログ |
| 書き手の立場 | 事実として説明することと、筆者の判断として述べること |
| 未確認事項 | 追加で確かめること、確認できない場合に記事から外すこと |
| 図表の設計 | 各図表で伝えること。図形の役割と配色。既定色を変える場合はその理由 |

主メッセージを一文で書けない場合は、情報収集かテーマの絞り込みが不足している。本文を書き始める前に整理する。

### 読者像は広げすぎない

「JavaScriptを使うすべての人」のような読者設定では、説明の深さを決められない。「JavaScriptの基本構文は読めるが、Promise連鎖の値の流れを説明できない人」のように、現在地と疑問を具体化する。

### 一記事で解決する中心課題は一つにする

関連する内容を網羅するために、複数の大きなテーマを同じ記事に詰め込まない。中心課題から外れる内容は、補足として短く扱うか別記事へ分ける。

### 本文を書く前に、記事の芯を短く書く

設計メモは、最低でも次の形まで具体化する。

```text
想定読者:
いま困っていること:
読了後にできること:
この記事の結論:
扱わないこと:
見せる具体例:
筆者が実際に確認したこと:
筆者の判断として書くこと:
未確認事項:
図表で伝えること:
配色（主役・補助・外部・注意・失敗）:
```

「筆者が実際に確認したこと」が空欄なら、検証前の一般論だけで書こうとしている可能性がある。小さくてもよいので、コードを動かす、ログを見る、複数案を比べるといった観測を一つ作ってから本文へ進む。

読者や中心課題が決まらない場合は、記事の責任者へ確認し、本文を書き始めない。再現に必要な情報が足りなければ、最小コード、実行コマンド、期待した結果、実際の結果、環境とバージョン、根拠URLを集める。周辺的な未確認事項は対象外として明記できるが、中心となる主張を実行結果か一次情報で確認できない記事は完成としない。

## 3. タイトルとフロントマター

### タイトル

タイトルだけで「対象技術」と「読者が得る理解」が分かる形にする。

良い例：

- `JavaScriptのPromiseを図で理解する：値・エラー・処理順のつながり`
- `npm auditの読み方：--forceを使う前に確認すること`
- `PATHとは？「command not found」を仕組みから直す`

避ける例：

- `Promiseについて`
- `npm audit完全解説`
- `初心者でも絶対わかるPATH`

誇張表現や内容を特定できないラベルは使わない。「入門」「完全版」「徹底解説」を付ける場合は、その範囲を本文で満たせるか確認する。

### Zennのフロントマター

フロントマターは、記事のタイトル、種類、トピック、公開状態などをZennへ伝えるためのメタデータである。

```yaml
---
title: "記事タイトル"
emoji: "🔎"
type: "tech"
topics: ["javascript", "デバッグ"]
published: false
---
```

- `title` は記事内容と一致させる。
- `emoji` は記事の識別に使えるものを一つ選ぶ。
- `topics` は主要な技術や分野に絞る。検索目的の無関係なタグを足さない。
- 執筆中は `published: false` とし、公開準備が完了してから変更する。

## 4. 記事の基本構成

次の項目は、記事で答える問いを並べたものであり、そのまま見出しとして使うためのテンプレートではない。短い記事では統合し、テーマに合わない項目は省略する。

```text
1. 冒頭：問題、結論、この記事で分かること
2. 全体像：仕組みを図または短い表で示す
3. 最小例：中心となる挙動だけを確認する
4. 詳細：処理順、データ、責務を段階的に説明する
5. 実務上の判断：選択基準、失敗、例外、限界を扱う
6. 確認手順：不具合や理解のずれを切り分ける
7. 結び：記事全体を一つの判断基準へ再統合する
8. 参考資料：仕様と公式ドキュメントを示す
```

### 冒頭では問題と到達点を示す

最初の二〜三段落で、読者が読むべき記事か判断できるようにする。

- 何が分かりにくいのか。
- よくある誤解や失敗は何か。
- この記事を読むと何を説明・判断できるようになるのか。

「この記事では〜を解説します」だけで始めない。対象となる問題や結論を先に置く。

### 見出しは内容を予告する

`概要`、`詳細`、`まとめ` だけでは、見出しから内容を判断できない。見出しを読めば、その節の要点が分かる形にする。

- `thenは元のPromiseを変えず、次のPromiseを返す`
- `PATHへ入れるのは実行ファイルではなく親ディレクトリ`
- `補償処理は失敗する前提で設計する`

原則として見出し階層は `##` と `###` までに収める。細かく分割しすぎて本文が一〜二文だけになる場合は、前後の節と統合する。

### 節ごとに説明の厚みを変える

中心となる仕組み、誤解しやすい箇所、失敗時の挙動は厚く説明する。補足的なAPI一覧や関連技術は短く扱う。全節を同じ長さ、同じ段落数、同じ書き出しにそろえない。

## 5. 説明文の要件

### コードの前後に説明を置く

コードブロックだけで説明を終えない。主要なコード例には、原則として次の情報を添える。

| 位置 | 説明すること |
| --- | --- |
| コードの前 | 何を確かめる例か。入力や前提は何か |
| コードの直後 | どの順序で動くか。重要な行はどこか |
| 結果の後 | 何が分かり、次の判断へどう使うか |

たとえばログの例なら、出力項目を紹介するだけで終えない。「この値ならAPI側を調べる」「入口ですでに値が違えばフォーム変換を追う」のように、観測結果から次の行動まで説明する。

### コードは処理順に読み解く

重要な例では、次の観点を文章か表で追う。

1. 入力される値
2. 最初に実行される処理
3. 状態や値が変わる地点
4. 戻り値または出力
5. 例外・失敗時の経路
6. 呼び出し側が受け持つ責任

非同期処理では、開始順、完了条件、エラー伝播、キャンセル、後始末を確認する。状態を持つ処理では、各段階で何が確定し、失敗時に何を戻す必要があるかを示す。

### 専門用語は初出で説明する

略語や専門用語は、初出で機能や意味を説明してから名前を使う。

- `APIなどの境界を越えてデータを受け渡す形式を、DTO（Data Transfer Object）と呼びます。`
- `変数名と、その名前が指す値を結び付ける仕組みを束縛と呼びます。`

公式API名、識別子、コマンド、ファイル名はインラインコードで示す。一般語として日本語にできるものを、生の英単語のまま本文へ混ぜない。

- 推奨：オプション、リスク、パッケージ、依存ツリー、リクエスト
- 避ける：option、risk、package、tree、request

固有の技術用語として英語表記が必要な場合は、最初に日本語の説明を付ける。

### 日本語は一文一義を基本にする

- 一文に複数の結論を詰め込まない。
- 主語と述語を離しすぎない。
- 読点は息継ぎではなく、意味の切れ目へ置く。
- 一〜二文だけの段落を機械的に繰り返さない。
- 短い文と長い文を混ぜ、単調なリズムを避ける。
- 「〜することができます」は、必要がなければ「〜できます」と書く。
- 「〜ではなく、〜です」という対比を繰り返さない。
- 同じ結論を表現だけ変えて繰り返さない。

### 文体は「丁寧だが、講義調にしすぎない」

本文は `です・ます` を基調にする。ただし、すべての文末を同じ形へそろえない。要点を示す短文、問い、体言止めは、流れを変えたい場所に限って使ってよい。

```text
通常の説明: この時点では、まだ処理は始まっていません。
短い要点: 問題は、ここからです。
次の疑問: では、どの時点で値が確定するのでしょうか。
観測した事実: 手元の環境では、ログは1回だけ出ました。
筆者の判断: この用途なら、私は逐次実行を選びます。
```

親しみやすさを出すためだけの挨拶、感嘆符、絵文字、読者への過剰な呼びかけは足さない。読者の疑問を先回りして答える方が、距離は自然に縮まる。

### 人らしさは、具体性と判断から生まれる

人が書いたように見せるための飾りは不要である。書き手の輪郭は、次の三つから見えてくる。

1. **観測**：実際に動かした結果、表示されたログ、起きた失敗、確認した仕様
2. **解釈**：その結果から何が分かるのか
3. **判断**：どの条件なら採用し、どの条件なら避けるのか

たとえば「この方法には柔軟性があります」で終えると、誰にでも書ける一般論になる。「リスナーを後から増やせます。ただし、呼び出し順がコードから見えにくくなるので、反応が1つだけなら直接呼びます」まで書けば、選択基準が残る。

数字、名前、入出力、エラーメッセージ、比較した案を優先して使う。「さまざまな場面」「適切に処理する」「柔軟に対応する」のような抽象語は、何を指すか具体化できないなら削る。

### 実体験は創作しない

`私は〜でハマりました`、`実務でよく遭遇します`、`数か月使って気づきました`のような一人称は、記事の名義人から提供された事実だけに使う。AIや検証ツールが実行した結果は、`Node.js 22で確認すると〜`のように観測条件を主語にし、筆者の体験へ置き換えない。人間らしさを演出するために、体験、感情、所要時間、現場の反応を補ってはいけない。

実体験がない場合も、不自然に隠す必要はない。観測した範囲をそのまま書く。

| 根拠 | 自然な書き方 |
| --- | --- |
| 自分で再現した | `Node.js 22で試すと、2回目の呼び出しでは同じ値が返りました。` |
| 公式仕様で確認した | `仕様では、この場合に新しいPromiseを返すと定められています。` |
| 一般的な失敗例として示す | `たとえば、解除処理を忘れるとリスナーが残ります。` |
| 筆者の好み・判断 | `小さな画面内の通知なら、私はまず直接呼び出せないかを考えます。` |
| 確証がない | `ここから先は推測です。実装によって変わる可能性があります。` |

### AIに見えやすい言い回しを、そのまま使わない

次の表は置換辞書ではない。左側の表現を見つけたら、その文が本当に必要かを考える。不要なら言い換えずに削る。

| AIに見えやすい文 | 書き直す方向 |
| --- | --- |
| `本記事では〜について詳しく解説します` | 読者が困る場面か、先に伝えたい結論から始める |
| `〜は非常に重要です` | 何が壊れるのか、どの判断に効くのかを書く |
| `〜することができます` | `〜できます`、または実際に起きる動作を書く |
| `〜と言えるでしょう` | 根拠があれば言い切り、なければ条件や不確実さを書く |
| `以下のとおりです` | 前置きを消し、内容を直接置く |
| `メリットとデメリットがあります` | 得るものと失うものを、その技術固有の言葉で書く |
| `適切に使い分けることが大切です` | どの条件で何を選ぶかを書く |
| `まず、次に、最後に`の反復 | 本当に順序が必要な箇所だけに残す |
| 各節末の`〜が重要です` | 次の疑問、判断、具体例へそのまま接続する |
| `いかがでしたか`、`ぜひ試してみてください` | 結論と、読者が次に確認することを示して終える |

`一方で`、`また`、`さらに`、`つまり`も禁止語ではない。ただし、段落の頭へ機械的に置くと文章のリズムがそろう。接続詞を消しても意味が通るなら、消した方が自然になることが多い。

### 書き換えは、口調より情報を変える

#### 冒頭

書き換え前：

> DIは、現代のソフトウェア開発において非常に重要な設計手法です。本記事では、DIの概要からメリットまで詳しく解説します。

書き換え後：

> 依存が1つ増えただけなのに、呼び出し側のコードまで直すことになった。DIは、この変更をオブジェクトの生成側へ閉じ込めるための仕組みです。

書き換え後には、困りごととDIの役割が書かれている。読者は、自分に必要な記事かを冒頭で判断できる。

#### 判断

書き換え前：

> 状況に応じて、適切な方法を選択することが重要です。

書き換え後：

> 反応が1つなら直接呼びます。通知先が増え、追加のたびに変更元を直すようになったらObserverを検討します。

「場合による」で逃げず、選ぶ境目まで書くと筆者の判断が見える。

#### 実体験がない場合

書き換え前：

> 私も購読解除を忘れて、何度もメモリリークに悩まされました。

書き換え後：

> 購読を解除しないまま画面を破棄すると、Subjectがリスナーを保持し続けます。画面を開き直すたびに通知回数が増えるなら、まず解除処理を確認します。

架空の経験を足さなくても、起きる現象と確認方法を書けば文章は具体的になる。

### 段落の型をそろえすぎない

すべての節を「定義→コード→メリット→注意点」の順で書くと、内容が違っても同じ記事を繰り返しているように見える。中心部分はコードを追い、判断が難しい部分は比較から入り、失敗が重要な部分はエラーから始める。内容に合わせて入口を変える。

段落も毎回三文にそろえない。短い事実だけで足りる段落は短くし、誤解をほどく段落には説明を使う。整った均一さより、重要度に応じた濃淡を残す。

### 箇条書きは、並列な情報だけに使う

説明できる内容を何でも箇条書きへ逃がすと、文章が設計書のように平板になる。手順、確認項目、同じ軸の比較など、順序や並列性がある情報だけを箇条書きにする。因果関係や筆者の判断は、地の文でつなぐ。

## 6. サンプルコードの要件

### 一つのコード例で確かめることを絞る

コード例は、記事の中心となる疑問に答えるために置く。一つの例にログ、キャッシュ、再試行、認証など複数の論点を詰め込まない。

一つのコードブロックは、おおむね5〜30行を目安にする。40行を超える場合は、次を確認する。

- 説明対象と無関係な初期化や型定義を省けないか。
- 複数の論点を別の例へ分けられないか。
- 全体コードを掲載するより、リポジトリへリンクする方が適切ではないか。

行数は絶対条件ではない。分割すると処理の全体像を失う場合は、長いまま掲載し、読む区間を明示する。

### 抜粋であることを明示する

説明上不要な型や補助関数を省略した場合は、コード例の前にその旨を書く。未定義の名前が記事の誤りなのか、省略なのかを読者に判断させない。

例：

> コード例は通知契約を説明するための抜粋です。注文モデルや画面更新処理の詳細は省略しています。

### コード内の整合性を保つ

- 同じ概念に異なる単位を混ぜない。金額、時刻、距離などは特に注意する。
- 変数名、型名、エラー名を記事全体で統一する。
- 前の例で定義した関数を使う場合は、その関係を本文で示す。
- 成功例だけでなく、記事の主題に関係する失敗経路も扱う。
- TypeScriptの型が実行時検証になるような誤解を与えない。
- Promiseのタイムアウトと実処理のキャンセルを混同しない。
- リソースを生成する例では、破棄やライフサイクルも検討する。

### 実行結果を示す

挙動の理解が目的なら、期待する出力、戻り値、状態変化のいずれかを示す。結果を示せない例は、何を確認するコードなのか再検討する。

## 7. 図と表の要件

### 図は処理の関係や順序を見せる

テキスト記法から図を生成するMermaidは、次の内容を見せるときに向いている。

- 三段階以上の処理順
- 複数の要素へ分岐する依存関係
- 状態遷移
- 呼び出しと戻り値の方向
- オブジェクトやモジュールの階層

| 示したい内容 | 適する図 |
| --- | --- |
| 処理手順、条件分岐 | `flowchart` |
| 呼び出しと応答 | `sequenceDiagram` |
| 状態の変化 | `stateDiagram-v2` |
| 所有・継承・依存 | 階層または関係図 |

図の前後には、どこを見る図なのかを説明する。矢印の意味が曖昧な往復図や、本文・表と同じ情報を並べ直しただけの図は使わない。

### Mermaid図では、色と役割を対応させる

Mermaid図や自作図を複数置く場合は、記事ごとではなくプロジェクト全体で次の配色を共通利用する。同じ役割の図形は、別の図でも同じ色にする。ブランドカラーなどを優先して配色を変える場合は、設計メモへ色と役割を記録し、一つの記事の中で既定色と混在させない。

| 役割 | 主な用途 | 塗り | 枠線 | 文字 |
| --- | --- | --- | --- | --- |
| 主役 | 説明の中心、主な処理経路 | `#DBEAFE` | `#2563EB` | `#1E3A8A` |
| 補助 | 補助処理、正常な分岐 | `#CCFBF1` | `#0F766E` | `#134E4A` |
| 外部・中立 | 外部サービス、前提、未選択の要素 | `#F8FAFC` | `#64748B` | `#0F172A` |
| 注意 | 注意が必要な条件、回復可能な問題 | `#FEF3C7` | `#D97706` | `#78350F` |
| 失敗 | エラー、停止、回復不能な状態 | `#FEE2E2` | `#DC2626` | `#7F1D1D` |

通常の図は、主役・補助・外部の3役で組み立てる。注意と失敗は、その意味が図に存在するときだけ追加する。色だけで意味を伝えず、ラベル、図形、線の種類も併用する。ライト表示とダーク表示の両方で、文字、枠線、矢印を読めるか確認する。

### 表は比較と対応関係に使う

次の内容には表が向いている。

- APIや手段の比較
- 入力、処理、出力の対応
- 状態と意味の対応
- 選択条件と注意点
- 用語と定義

表のセルに長文を詰め込みすぎない。表だけでは判断理由が伝わらない場合は、重要な行を本文で補足する。

### 視覚要素の採用基準

全体像の把握に図が、比較や対応の確認に表が役立つなら採用する。「一記事に各一つ」のような数合わせはしない。採用した図表が読者の再読を減らしているかを確認する。

## 8. 記事タイプごとの重点

| 記事タイプ | 厚く説明する内容 |
| --- | --- |
| 概念・言語仕様 | 最小例、評価順、似た概念との違い |
| ハウツー | 前提、手順、成功確認、元に戻す方法 |
| デバッグ | 仮説、観測点、結果から次の調査へ進む基準 |
| 設計パターン | 解決する変更理由、契約、組み立て方、使わない条件 |
| 非同期処理 | 開始・完了・失敗・キャンセル・後始末 |
| セキュリティ | 攻撃条件、到達可能性、影響、緩和策、残存リスク |
| 内部実装・性能 | 仕様と実装詳細の区別、計測条件、断定できる範囲 |

設計パターンの記事では、クラス図や実装方法だけを説明しない。導入前の問題、導入後に利用側から消える判断、順序や失敗によって変わる契約まで扱う。

## 9. 技術的正確性と参考資料

### 事実の根拠を分ける

本文では、次の三つを混同しない。

1. 言語仕様や公式APIが保証する挙動
2. V8など特定実装が選ぶ内部構造
3. 記事内で提案する設計判断

実装依存の内容には、対象バージョンや計測条件を付ける。「常に速い」「絶対に安全」のような断定は、根拠がある場合だけ使う。

### 参考資料は一次情報を優先する

- 言語仕様
- 公式ドキュメント
- 標準化団体の文書
- ライブラリの公式ガイド
- 公式リポジトリ

外部記事は補助資料として扱う。リンク先が本文の主張を直接支えているか、公開前に開いて確認する。似た名前の別概念へリンクしない。

## 10. 表現の修正だけでは直らない兆候

次の状態を見つけたら、言い換えを続けず、設計か検証へ戻る。

- 中心となる主張に、実行結果も一次情報もない。
- コードや図表が続くのに、観測結果から次の判断へ進む説明がない。
- 全節が同じ長さと構成で並び、どこが重要なのか分からない。
- 仕様、実装詳細、筆者の判断が同じ確度で書かれている。
- 根拠のない体験談や現場の反応が、一人称で補われている。

## 11. 作成手順

### 1. 設計する

「2. 執筆前に決めること」の設計メモを作る。設計メモは作業用であり、そのまま記事本文へ貼り付けない。実際に確認した事実と、筆者の判断として書くことを分け、観測材料がなければ先に小さな検証を行う。

### 2. 見出しだけで論旨を作る

見出しを順に読み、問題から理解、実践、判断へ進めるか確認する。本文を書く前に、重複する節や不足する節を直す。

### 3. コード・図・表の役割を割り当てる

各要素に「何を理解させるか」を一文で付ける。同じ役割の要素が複数ある場合は統合する。図を使う場合は、図形の役割と共通の配色もここで決める。

### 4. 重要な節から書く

中心となる挙動、実務上の失敗、判断基準を先に書く。冒頭と結びは、本文の内容が固まってから調整する。

最初から整った記事にしようとしない。まず、観測したことと自分の判断を具体的に書く。言葉の重複や段落の形は、内容が出そろってから直す。

### 5. 技術検証を行う

コードの構文、JSON、コマンド、出力、リンクを確認する。実行環境とバージョン、実行したコマンド、期待した結果、実際の結果を作業メモへ残す。外部サービスを使う例は、失敗条件と後始末も確認する。

実行できなかった検証を、完了したように書かない。中心となる主張に未確認事項が残る場合は完成とせず、確認できる範囲へ記事を絞る。

### 6. 読みやすさと筆者の声を確認する

見出しと各段落の最初の文だけを読んで論旨を確認する。その後、本文を通読し、説明不足、用語の未定義、英単語の混在、文の単調さを直す。

続けて、次の観点だけでもう一度読む。

- 誰にでも書ける一般論で終わっている段落はないか。
- 実行結果、具体例、比較条件のいずれかを足せないか。
- 事実と筆者の判断を同じ確度で書いていないか。
- 一人称の体験や感情を創作していないか。
- 全節を同じ型、同じ長さ、同じ語調へそろえていないか。
- 言い換えただけの重複を、思い切って削れないか。

`natural-japanese` を利用できる環境では、次の検査も実行する。

```sh
python3 ~/.codex/skills/natural-japanese/scripts/lint.py --json --genre tech articles/<slug>.md
python3 ~/.codex/skills/natural-japanese/scripts/outline.py articles/<slug>.md
python3 ~/.codex/skills/natural-japanese/scripts/terms.py articles/<slug>.md
```

検査結果は、推敲する箇所を見つけるための判断材料として使う。自動修正せず、指摘された文を前後の段落と一緒に読み、意味が明確か、記事全体のリズムを損ねていないかを確認してから直す。各指摘を「修正」か「残す（理由）」へ分け、修正後に再実行して新しい指摘が増えていないことを確かめる。`outline.py` は見出しと段落先頭だけで論旨が通るか、`terms.py` は説明が必要な用語を初出で扱えているかを判断するために使う。

### 7. Zennで表示確認する

```sh
pnpm check
git diff --check
```

別のターミナルでプレビューを起動する。

```sh
pnpm preview
```

記事ページを開き、次を確認する。

- フロントマターが正しく読まれる。
- コードフェンスが閉じている。
- Mermaid図と表が崩れていない。
- 図形の色と意味が全図で一致し、ライト表示とダーク表示の両方で読める。
- 見出し階層が正しい。
- 横に長いコードや表が読める。
- 参考リンクが開く。

## 12. 公開前の完了条件

該当する項目をすべて確認できたら完成とする。記事の種類に合わない項目は無理に満たさず、設計メモへ `N/A` と理由を残す。

### 内容

- [ ] 想定読者と中心課題を一文で説明できる。
- [ ] 冒頭で問題と読了後の状態が分かる。
- [ ] 見出しだけで論旨を追える。
- [ ] コードを載せる場合、中心となるコードの入力、処理順、出力を説明している。
- [ ] 中心課題に関係する失敗経路、限界、使わない条件を扱っている。
- [ ] 結びが実務上の判断基準へつながっている。

### コード・図・表

- [ ] コードを載せる場合、コードブロックの前後に説明がある。
- [ ] コードを抜粋する場合、省略した型や補助処理を明示している。
- [ ] 掲載したコード、JSON、コマンドの構文を確認した。
- [ ] 図表を使う場合、各図表に役割があり、本文と重複していない。
- [ ] 図を使う場合、同じ役割の図形は同じ色で、色がなくてもラベルや形から意味を追える。
- [ ] 単位、日時、型名、変数名が記事内で統一されている。

### 文章

- [ ] 専門用語を初出で説明している。
- [ ] 一般語を不要な英単語のまま残していない。
- [ ] 一文に複数の結論を詰め込んでいない。
- [ ] 同じ構文や段落の型を繰り返していない。
- [ ] 読者が結果から次の判断へ進める説明がある。
- [ ] 抽象的な評価語を、具体的な結果や条件へ置き換えている。
- [ ] 観測、解釈、判断のうち、その段落に必要なものが書かれている。
- [ ] 筆者の経験、感情、現場の反応を創作していない。
- [ ] `本記事では`、`重要です`、`と言えるでしょう`などの定型句に頼っていない。
- [ ] 文や節の長さに、内容の重要度に応じた濃淡がある。

### 根拠・表示

- [ ] 仕様、実装詳細、設計判断を区別している。
- [ ] 重要な主張を一次情報で確認した。
- [ ] 実行できなかった検証を未確認として明示し、中心となる主張に未確認事項が残っていない。
- [ ] 参考リンクが有効で、本文の内容と一致する。
- [ ] Zennの解析とプレビューで表示を確認した。

## 13. 再利用用の執筆指示

新しい記事を作るときは、次の指示を起点にできる。

```text
ARTICLE_REQUIREMENTS.mdに従って、Zenn向けの技術記事を作成してください。

最初に「2. 執筆前に決めること」の設計メモを作業用に提示し、確認済みの事実、筆者の判断、未確認事項を分けてください。素材が足りなければ創作で埋めず、必要な情報を質問してください。
設計後に見出しを作り、コード・説明・図表の役割を決めてから本文を書いてください。図を使う場合は、ガイドの共通配色を使い、同じ役割の図形に同じ色を割り当ててください。
本文は「です・ます」を基調にし、定型句、抽象的な評価、均一な節構成を避けてください。実行結果、具体例、採用条件、使わない条件を優先し、体験や感情は創作しないでください。
完成後は該当する完了条件を検証し、実行できなかった確認を完了扱いにせず、未確認事項として報告してください。
```

この指示は記事の骨格を安定させるためのものだが、完成形を一律にするものではない。読者の疑問に合わせて説明の順序と厚みを調整し、その記事で最も重要な部分へ説明量を配分する。
